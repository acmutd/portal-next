import React from 'react';

const NAME: React.FC = () => {
  const msg = 'This is a template component that you can copy in order to create a new component';
  return <div>{msg}</div>;
};

export default NAME;
